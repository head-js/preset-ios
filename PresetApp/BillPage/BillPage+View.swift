import SwiftUI

extension BillPage {
    struct View: SwiftUI.View {
        var body: some SwiftUI.View {
            Text("Bill Page")
                .font(.title)
                .frame(maxWidth: .infinity, maxHeight: .infinity)
        }
    }
}
